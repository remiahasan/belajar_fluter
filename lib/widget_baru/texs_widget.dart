import 'package:flutter/material.dart';

class TexsWiget extends StatelessWidget {
  const TexsWiget({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return const Text("belajar fluter", style: TextStyle(color: Colors.lime, fontSize: 40), textAlign: TextAlign.center,);
  }
}
